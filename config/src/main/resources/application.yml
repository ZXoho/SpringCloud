spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ZXoho/config
          username: ZXoho
          password: pineapple8799
          basedir: H:\project\spring cloud\config\basedir
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"